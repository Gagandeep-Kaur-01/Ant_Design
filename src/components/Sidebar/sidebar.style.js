import styled from "styled-components";

const SidebarWrapper = styled.div`

    .sidebarHeading {
      height: 50px;
      flex: 0 0 280px;
    }
    .sidebarHeading h2 {
        color: #fff;
        font-size: 26px;
        padding: 5px 30px;
    }

`;

export default SidebarWrapper;